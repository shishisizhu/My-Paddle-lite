if(NOT LITE_WITH_LOONGARCH)
  return()
endif()
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -mlasx")
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -mlasx")
FILE(GLOB LOONGARCH_MATH_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.cc)
 set_source_files_properties (${LOONGARCH_MATH_SRC} PROPERTIES COMPILE_FLAGS "-mlasx")
lite_cc_library(loongarch_math SRCS ${LOONGARCH_MATH_SRC})
include_directories(${CMAKE_CURRENT_SOURCE_DIR})
